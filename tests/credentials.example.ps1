# Copy this file to credentials.ps1 and fill in your values.
# credentials.ps1 is git-ignored and will never be committed.

$testUsername  = 'user@example.com'
$testPassword  = 'secret'
$testAccountId = 0          # a device AccountId to use for per-device tests
